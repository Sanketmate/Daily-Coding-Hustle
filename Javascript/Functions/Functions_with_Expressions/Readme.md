✅ Assignment Set: Function Expressions
🟢 Level 1: Basic Practice

Create a function expression sayHi that prints:

Hi, User!


Create a function expression add that returns sum of two numbers.

Create a function expression multiply that returns product of two numbers.

Create a function expression printAge that prints your age.

🟡 Level 2: With Parameters & Return

Create a function expression square that returns square of a number.

Create a function expression findMax that returns bigger number between two numbers.

Create a function expression isPositive that returns true if number is positive.

Create a function expression convertToUpper that converts string to uppercase.

🟠 Level 3: Logic Building

Create a function expression factorial to calculate factorial.

Create a function expression reverseString to reverse a string.

Create a function expression countDigits to count digits in a number.

Create a function expression isPalindrome to check palindrome string.

🔵 Level 4: Using Function Expressions in Programs

Create a calculator using function expressions:

add(), sub(), mul(), div()


Create a function expression findAverage for array numbers.

Create a function expression findEvenNumbers from an array.

Create a function expression sumArray to sum array elements.

🔴 Level 5: Advanced Usage

Create a function expression that accepts another function (callback).

Create a function expression using setTimeout.

Create a function expression that returns another function.

Create an IIFE (Immediately Invoked Function Expression).

📌 Example Format (Follow This)

Write every answer like this:

const functionName = function(parameters) {
   // logic
   return result;
};


Example:

const square = function(num) {
   return num * num;
};

🎯 Mini Challenge

Make a Student Result System using function expressions:

Functions:

totalMarks(m1,m2,m3)

percentage(total)

grade(percent)